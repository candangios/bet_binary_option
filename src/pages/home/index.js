import React, { memo } from "react";
import { Box } from "rebass";
import Footer from "../Footer"




export default memo(()=>{
    return(
        <Box>
            <Footer></Footer>

        </Box>
    )
})